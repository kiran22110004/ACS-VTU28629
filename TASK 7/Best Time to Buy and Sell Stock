//PROGRAM 
class Solution {
    public int maxProfit(int[] prices) {
        int minPrice = Integer.MAX_VALUE;
        int maxProfit = 0;

        for (int price : prices) {
            if (price < minPrice) {
                minPrice = price; // Track the lowest buying price seen so far
            } else if (price - minPrice > maxProfit) {
                maxProfit = price - minPrice; // Update the maximum profit if selling today
            }
        }

        return maxProfit;
    }
}


Accepted
Runtime: 0 ms
Case 1
Case 2
Input
prices =
[7,1,5,3,6,4]
Output
5
Expected
5
